# =============================================================================
# LOCAL DEVELOPMENT ONLY (for Wrangler CLI deployment)
# =============================================================================
# These are needed locally to deploy via wrangler CLI.
# Create token at: https://dash.cloudflare.com/profile/api-tokens
#
# Required permissions:
#   - D1: Edit
#   - Cloudflare Pages: Edit
#   - Workers R2 Storage: Edit
#   - Workers KV Storage: Edit
#   - Workers Scripts: Edit
#   - Account Settings: Read

CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here

# =============================================================================
# CLOUDFLARE WORKER SECRETS (Do NOT store locally - set via wrangler CLI)
# =============================================================================
# These secrets should be configured as Cloudflare Worker secrets:
#   wrangler secret put SHOTSTACK_API_KEY --name render-service
#
# Or via Dashboard → Workers → render-service → Settings → Variables → Secrets
#
# Required secrets:
#   SHOTSTACK_API_KEY   - Shotstack API key (https://dashboard.shotstack.io/)
#   SHOTSTACK_ENV       - Shotstack environment (stage/v1) - can be plain env var
