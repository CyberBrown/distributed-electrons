{
  "name": "@distributed-electrons/sandbox-executor",
  "version": "2.0.0",
  "description": "Cloudflare Worker for delegating Claude tasks to on-prem runner",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.53.0"
  }
}
